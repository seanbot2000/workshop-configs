security:
  basic:
    enabled: false

ribbon:
  eureka:
    enabled: false

zuul:
  add-proxy-headers: true
  ignored-services: "*"
  routes:
    employees:
      path: /api/employees/**
      url: https://rapid-tst.cerner.com:8243/emp/v1/api/employees
      stripPrefix: false